import { dateFormat } from './date';
import { now } from './common';
import {
	getTimeStamp,
	isTimeStamp,
	isSecondsTimeStamp,
	isMillisecondsTimeStamp,
} from './timestamp';

export {
	// 日期处理
	dateFormat,

	// timestamp处理
	now,
	getTimeStamp,
	isTimeStamp,
	isSecondsTimeStamp,
	isMillisecondsTimeStamp,
}
